#include <stdio.h>

int main(void)
{
  int nx;
  int *p;

  p = &nx;
  *p = 100;

  printf("nx = %d\n", nx);
  printf("*p = %d\n\n", *p);

  nx = 200;
  printf("nx = %d\n", nx);
  printf("*p = %d\n\n", *p);

  return 0;
}
